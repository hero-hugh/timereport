# Ralph Progress Log
Started: tis  3 mar 2026 21:04:25 CET

## Codebase Patterns
- UI components live in `apps/web/src/components/ui/` and use `React.forwardRef` pattern with `displayName`
- Use `cn()` from `../../lib/utils` (or `../lib/utils` depending on depth) for Tailwind class merging
- Button component has variants: default, destructive, outline, secondary, ghost, link and sizes: default, sm, lg, icon
- Responsive layout pattern: use `md:hidden` / `hidden md:block` for mobile vs desktop views
- Run `npx biome format --write <file>` after creating/editing files to fix formatting before lint check
- TypeScript check: `npx tsc --noEmit -p apps/web/tsconfig.json`
- Lint check: `npx biome check <file>`

---

## 2026-03-03 21:05 - HUG-25-1
- Created `UnsavedChangesBar.tsx` component in `apps/web/src/components/`
- Implements sticky action bar with slide-in/out animation (translate-y, 300ms ease-out)
- Desktop: fixed bottom bar with text left / buttons right
- Mobile: stacked layout with 44px min-height touch targets
- Amber warning color scheme (bg-amber-50/95, border-amber-300)
- Uses existing Button component with outline and default variants
- Files changed: `apps/web/src/components/UnsavedChangesBar.tsx` (new)
- **Learnings for future iterations:**
  - Biome formatter uses tabs and has specific trailing comma preferences — always run `npx biome format --write` after creating files
  - The `cn()` import path from `components/` is `../lib/utils`
  - Mobile/desktop split in this codebase uses `md:` breakpoint (768px)
---

## 2026-03-03 21:15 - HUG-25-2
- Integrated UnsavedChangesBar into TimePage.tsx with showActionBar state
- Added `onBlur` handlers on both mobile and desktop time Input fields
- Action bar appears only after blur when hasChanges is true (not on keystroke)
- `showActionBar` resets to false in `loadData()` after successful save
- UnsavedChangesBar rendered with `visible={showActionBar && hasChanges}`
- Files changed: `apps/web/src/pages/TimePage.tsx` (modified)
- **Learnings for future iterations:**
  - The `handleTimeChange` sets `hasChanges = true` on every keystroke but the action bar only shows on blur — this is the correct UX pattern
  - `loadData()` is the single place where `hasChanges` resets to false (called after save), so resetting `showActionBar` there covers all save paths
  - The old "Spara ändringar" button still exists alongside the new bar — Story 5 will remove it
---

## 2026-03-03 - HUG-25-3
- Verified Cancel ("Avbryt") button behavior already implemented in Story 2
- `onCancel={() => setShowActionBar(false)}` hides bar immediately
- `hasChanges` stays true, `weekData` untouched — changes preserved
- `handleTimeBlur` re-shows bar on next blur when `hasChanges` is true
- No code changes needed — all 5 requirements already satisfied
- Files changed: none (verification only)
- **Learnings for future iterations:**
  - The Cancel behavior was a natural side effect of Story 2's integration — `onCancel` only sets `showActionBar = false`, which preserves all data
  - When stories build on each other, check if prior work already satisfies later requirements before writing new code
---
